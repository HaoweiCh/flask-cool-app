services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      - FLASK_DEBUG=0  # 明确关闭调试
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
